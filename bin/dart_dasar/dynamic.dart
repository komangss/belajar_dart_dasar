void main() {
  // some times we need a variable that have a flexible data type; 
  dynamic variable = 100;
  print(variable);

  variable = "100";
  print(variable);

  variable = false;
  print(variable);

  // or you can use
  var variable2;
}