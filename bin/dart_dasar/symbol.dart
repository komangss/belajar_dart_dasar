// symbol is data type that rarely be used
// symbol can actually used for a constant

void main() {

  Symbol symbol1 = Symbol('Antonio Komang');
  var symbol2 = #belajar_dart;

  print(symbol1);
  print(symbol2);
}